Detected invocation by the agent. If you're an agent, you may use `builder goal set <objective>` only when no active or paused goal exists; a completed goal does not block starting the next goal. You are not allowed to overwrite an active or paused goal, and `builder goal pause`, `builder goal resume`, and `builder goal clear` remain user-only commands. Use `builder goal complete` if you can testify that it's done fully, or `builder goal show` to see the current goal, and do not try to circumvent the goal or this error anymore, or you will be penalized severely. If you're unable to complete the goal, use ask_question to ask the user for help. If you're a human, please consult goal docs to learn how to fix this.
