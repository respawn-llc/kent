name: release

on:
  workflow_dispatch:
  pull_request:
    types:
      - closed

jobs:
  prepare_release:
    runs-on: ubuntu-latest
    if: ${{ github.event_name == 'workflow_dispatch' || (github.event.pull_request.merged == true && contains(github.event.pull_request.labels.*.name, 'autorelease')) }}
    permissions:
      contents: write
    outputs:
      version: ${{ steps.version.outputs.version }}
      tag: ${{ steps.version.outputs.tag }}
      version_bumped: ${{ steps.version.outputs.version_bumped }}
      changelog: ${{ steps.changelog.outputs.changelog }}
      artifact_name: ${{ steps.artifact.outputs.name }}
    steps:
      - uses: actions/checkout@v7.0.0
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.base.ref || github.ref_name }}
          token: ${{ secrets.OPENSOURCE_PAT || github.token }}

      - uses: actions/setup-go@v6.5.0
        with:
          go-version-file: go.mod
          check-latest: true

      - name: Setup pnpm
        uses: pnpm/action-setup@v6.0.9
        with:
          version: 11.11.0

      - name: Setup Node
        uses: actions/setup-node@v6.4.0
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: apps/pnpm-lock.yaml

      - uses: extractions/setup-just@v3

      - name: Read version
        id: version
        env:
          GH_TOKEN: ${{ secrets.OPENSOURCE_PAT || github.token }}
          REPOSITORY: ${{ github.repository }}
        run: |
          version="$(tr -d ' \n' < VERSION)"
          if [ -z "$version" ]; then
            echo "VERSION file is empty" >&2
            exit 1
          fi
          version="${version#v}"
          if ! [[ "$version" =~ ^([0-9]+)\.([0-9]+)\.([0-9]+)$ ]]; then
            echo "VERSION must be a semantic version in X.Y.Z form, got: $version" >&2
            exit 1
          fi
          major="${BASH_REMATCH[1]}"
          minor="${BASH_REMATCH[2]}"
          patch="${BASH_REMATCH[3]}"
          tag="v${version}"

          version_bumped="false"
          while true; do
            draft_state="$(gh api "repos/${REPOSITORY}/releases/tags/${tag}" --jq '.draft' 2>/dev/null || echo missing)"
            if [ "$draft_state" != "false" ]; then
              break
            fi
            patch="$((patch + 1))"
            version="${major}.${minor}.${patch}"
            tag="v${version}"
            version_bumped="true"
          done

          if [ "$version_bumped" = "true" ]; then
            printf '%s\n' "$version" > VERSION
            echo "Published release already existed; bumped VERSION to $version for this release."
          fi

          {
            echo "version=$version"
            echo "tag=$tag"
            echo "version_bumped=$version_bumped"
          } >> "$GITHUB_OUTPUT"

      - name: Persist bumped VERSION before tagging
        if: ${{ steps.version.outputs.version_bumped == 'true' }}
        env:
          RELEASE_REF: ${{ github.event.pull_request.base.ref || github.ref_name }}
          RELEASE_VERSION: ${{ steps.version.outputs.version }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          printf '%s\n' "$RELEASE_VERSION" > VERSION
          git add VERSION

          if git diff --cached --quiet; then
            echo "No VERSION change to commit"
            exit 0
          fi

          git commit -m "chore: bump version to ${RELEASE_VERSION} [skip ci]"
          git push origin "HEAD:${RELEASE_REF}"

      - name: Create tag
        env:
          TAG: ${{ steps.version.outputs.tag }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          if git rev-parse "$TAG" >/dev/null 2>&1; then
            echo "Tag $TAG already exists"
          else
            git tag -a "$TAG" -m "$TAG"
            git push origin "$TAG"
          fi

      - name: Build changelog
        id: changelog
        uses: mikepenz/release-changelog-builder-action@v6.2.3
        with:
          commitMode: true
          configuration: ".github/changelog_config.json"

      - name: Build release archives
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: just release _cli-build --version "$VERSION" --dist-dir dist

      - name: Validate Linux release binaries are statically linked
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: just release _cli-verify-linux-static --version "$VERSION" --dist-dir dist

      - name: Capture release artifact name
        id: artifact
        env:
          TAG: ${{ steps.version.outputs.tag }}
        run: echo "name=release-assets-${TAG}" >> "$GITHUB_OUTPUT"

      - name: Upload release artifacts
        uses: actions/upload-artifact@v7.0.1
        with:
          name: ${{ steps.artifact.outputs.name }}
          path: dist/*
          if-no-files-found: error

  verify_release_manifest:
    needs: prepare_release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7.0.0
        with:
          persist-credentials: false

      - uses: extractions/setup-just@v3

      - name: Download release artifacts
        uses: actions/download-artifact@v8.0.1
        with:
          name: ${{ needs.prepare_release.outputs.artifact_name }}
          path: dist

      - name: Verify release checksums
        run: just release _cli-verify-manifest --dist-dir dist

  smoke_release_assets:
    needs: prepare_release
    strategy:
      fail-fast: false
      matrix:
        include:
          - runs_on: ubuntu-latest
            goos: linux
            goarch: amd64
            archive_ext: tar.gz
            binary_ext: ""
          # GitHub does not currently provide an ubuntu-latest arm64 alias.
          - runs_on: ubuntu-24.04-arm
            goos: linux
            goarch: arm64
            archive_ext: tar.gz
            binary_ext: ""
          - runs_on: macos-latest
            goos: darwin
            goarch: arm64
            archive_ext: tar.gz
            binary_ext: ""
          - runs_on: windows-latest
            goos: windows
            goarch: amd64
            archive_ext: zip
            binary_ext: .exe
          # GitHub does not currently provide a windows-latest arm64 alias.
          - runs_on: windows-11-arm
            goos: windows
            goarch: arm64
            archive_ext: zip
            binary_ext: .exe
    runs-on: ${{ matrix.runs_on }}
    defaults:
      run:
        shell: bash
    env:
      JUST_VERSION: 1.58.0
      JUST_WINDOWS_ARM64_SHA256: 3a39ed629eb67678976c811a4da46f7985a2c22f4dbabe017b8b2eb5ceb5d01c
    steps:
      - uses: actions/checkout@v7.0.0
        with:
          persist-credentials: false

      - uses: extractions/setup-just@v3
        if: matrix.runs_on != 'windows-11-arm'
        with:
          just-version: ${{ env.JUST_VERSION }}

      - name: Install just on Windows ARM
        if: matrix.runs_on == 'windows-11-arm'
        shell: pwsh
        run: |
          $assetName = "just-$env:JUST_VERSION-aarch64-pc-windows-msvc.zip"
          $archive = Join-Path $env:RUNNER_TEMP "just-windows-arm64.zip"
          $installRoot = Join-Path $env:RUNNER_TEMP "just-windows-arm64"
          Invoke-WebRequest `
            -Uri "https://github.com/casey/just/releases/download/$env:JUST_VERSION/$assetName" `
            -OutFile $archive

          $actualHash = (Get-FileHash -Path $archive -Algorithm SHA256).Hash.ToLowerInvariant()
          if ($actualHash -ne $env:JUST_WINDOWS_ARM64_SHA256) {
            throw "Windows ARM64 just archive SHA-256 mismatch: expected $env:JUST_WINDOWS_ARM64_SHA256, got $actualHash"
          }

          Expand-Archive -Path $archive -DestinationPath $installRoot

          $executables = @(Get-ChildItem -Path $installRoot -Filter just.exe -Recurse)
          if ($executables.Count -ne 1) {
            throw "Expected exactly one just.exe, found $($executables.Count)"
          }
          $executables[0].DirectoryName | Out-File -FilePath $env:GITHUB_PATH -Append
          & $executables[0].FullName --version

      - name: Download release artifacts
        uses: actions/download-artifact@v8.0.1
        with:
          name: ${{ needs.prepare_release.outputs.artifact_name }}
          path: dist

      - name: Smoke test packaged binary
        env:
          VERSION: ${{ needs.prepare_release.outputs.version }}
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          ARCHIVE_EXT: ${{ matrix.archive_ext }}
          BINARY_EXT: ${{ matrix.binary_ext }}
        run: |
          args=(
            --version "$VERSION"
            --goos "$GOOS"
            --goarch "$GOARCH"
            --archive-ext "$ARCHIVE_EXT"
            --dist-dir dist
          )
          if [[ -n "$BINARY_EXT" ]]; then
            args+=(--binary-ext "$BINARY_EXT")
          fi
          just release _cli-smoke "${args[@]}"

      - name: Smoke test Windows installer
        if: ${{ matrix.goos == 'windows' }}
        env:
          VERSION: ${{ needs.prepare_release.outputs.version }}
          GOARCH: ${{ matrix.goarch }}
        run: |
          just release _windows-installer-smoke \
            --version "$VERSION" \
            --goarch "$GOARCH" \
            --dist-dir dist

  publish_release:
    needs:
      - prepare_release
      - verify_release_manifest
      - smoke_release_assets
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7.0.0
        with:
          persist-credentials: false

      - name: Download release artifacts
        uses: actions/download-artifact@v8.0.1
        with:
          name: ${{ needs.prepare_release.outputs.artifact_name }}
          path: dist

      # Stays a draft until publish_desktop uploads the desktop bundles and flips it
      # public, so a failed desktop build leaves a re-runnable draft (reused by
      # prepare_release) instead of a public release missing desktop assets.
      - name: Create or update release
        uses: softprops/action-gh-release@v3.0.1
        with:
          tag_name: ${{ needs.prepare_release.outputs.tag }}
          name: ${{ needs.prepare_release.outputs.tag }}
          draft: true
          body: ${{ needs.prepare_release.outputs.changelog }}
          files: dist/*

  build_desktop:
    needs: prepare_release
    strategy:
      fail-fast: false
      matrix:
        include:
          - runs_on: macos-latest
            platform: macos
          - runs_on: ubuntu-22.04
            platform: linux
          - runs_on: windows-latest
            platform: windows
    runs-on: ${{ matrix.runs_on }}
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v7.0.0
        with:
          persist-credentials: false

      - name: Install Linux bundling dependencies
        if: matrix.platform == 'linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev \
            patchelf libfuse2 file build-essential

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2.9.1
        with:
          workspaces: apps/desktop/src-tauri

      - uses: actions/setup-go@v6.5.0
        with:
          go-version-file: go.mod
          check-latest: true

      - name: Setup pnpm
        uses: pnpm/action-setup@v6.0.9
        with:
          version: 11.11.0

      - name: Setup Node
        uses: actions/setup-node@v6.4.0
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: apps/pnpm-lock.yaml

      - uses: extractions/setup-just@v3

      - name: Install desktop dependencies
        run: just install _apps-dependencies

      - name: Prepare Apple notarization key
        if: matrix.platform == 'macos'
        env:
          APPLE_API_KEY_P8: ${{ secrets.APPLE_API_KEY_P8 }}
        run: |
          test -n "$APPLE_API_KEY_P8"
          umask 077
          key_path="$RUNNER_TEMP/kent-notarization.p8"
          printf '%s' "$APPLE_API_KEY_P8" > "$key_path"
          echo "APPLE_API_KEY_PATH=$key_path" >> "$GITHUB_ENV"

      - name: Build desktop bundles
        env:
          KENT_VERSION: ${{ needs.prepare_release.outputs.version }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
          # Tauri signs with Developer ID, notarizes with Apple, and staples the ticket.
          APPLE_CERTIFICATE: ${{ matrix.platform == 'macos' && secrets.APPLE_CERTIFICATE || '' }}
          APPLE_CERTIFICATE_PASSWORD: ${{ matrix.platform == 'macos' && secrets.APPLE_CERTIFICATE_PASSWORD || '' }}
          APPLE_SIGNING_IDENTITY: ${{ matrix.platform == 'macos' && secrets.APPLE_SIGNING_IDENTITY || '' }}
          APPLE_API_KEY: ${{ matrix.platform == 'macos' && secrets.APPLE_API_KEY || '' }}
          APPLE_API_ISSUER: ${{ matrix.platform == 'macos' && secrets.APPLE_API_ISSUER || '' }}
        run: just release _desktop-build --version "$KENT_VERSION" --dist-dir dist/desktop --skip-install

      - name: Verify macOS notarization
        if: matrix.platform == 'macos'
        run: |
          codesign --verify --deep --strict apps/desktop/src-tauri/target/release/bundle/macos/Kent.app
          xcrun stapler validate apps/desktop/src-tauri/target/release/bundle/macos/Kent.app

      - name: Remove Apple notarization key
        if: always() && matrix.platform == 'macos'
        run: rm -f "$RUNNER_TEMP/kent-notarization.p8"

      - name: Upload desktop artifacts
        uses: actions/upload-artifact@v7.0.1
        with:
          name: desktop-assets-${{ matrix.platform }}-${{ needs.prepare_release.outputs.tag }}
          path: dist/desktop/*
          if-no-files-found: error

  publish_desktop:
    needs:
      - prepare_release
      - publish_release
      - build_desktop
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7.0.0
        with:
          persist-credentials: false

      - uses: extractions/setup-just@v3

      # All three platforms' staged bundles merge into one dist dir so assemble sees
      # every updater .sig file and emits one cross-platform latest.json.
      - name: Download macOS desktop artifacts
        uses: actions/download-artifact@v8.0.1
        with:
          name: desktop-assets-macos-${{ needs.prepare_release.outputs.tag }}
          path: dist/desktop

      - name: Download Linux desktop artifacts
        uses: actions/download-artifact@v8.0.1
        with:
          name: desktop-assets-linux-${{ needs.prepare_release.outputs.tag }}
          path: dist/desktop

      - name: Download Windows desktop artifacts
        uses: actions/download-artifact@v8.0.1
        with:
          name: desktop-assets-windows-${{ needs.prepare_release.outputs.tag }}
          path: dist/desktop

      - name: Assemble updater manifest + checksums
        env:
          KENT_VERSION: ${{ needs.prepare_release.outputs.version }}
          TAG: ${{ needs.prepare_release.outputs.tag }}
        run: |
          just release _desktop-assemble \
            --version "$KENT_VERSION" \
            --dist-dir dist/desktop \
            --base-url "https://github.com/${{ github.repository }}/releases/download/${TAG}"

      # Uploads the desktop bundles and flips the release public (draft: false) — the
      # last step, so the release only goes live once every platform's assets exist.
      - name: Upload desktop assets and publish release
        uses: softprops/action-gh-release@v3.0.1
        with:
          tag_name: ${{ needs.prepare_release.outputs.tag }}
          draft: false
          files: dist/desktop/*

  update_brew_tap:
    needs:
      - prepare_release
      - publish_release
      - publish_desktop
    runs-on: ubuntu-latest
    env:
      BREW_FORMULA_NAME: kent
    steps:
      - uses: actions/checkout@v7.0.0
        with:
          fetch-depth: 0
          persist-credentials: false

      - uses: extractions/setup-just@v3

      - uses: actions/checkout@v7.0.0
        with:
          repository: respawn-llc/homebrew-tap
          path: homebrew-tap
          token: ${{ secrets.RESPAWN_BREW_TAP_TOKEN }}

      - name: Configure git
        run: |
          git -C homebrew-tap config user.name "github-actions[bot]"
          git -C homebrew-tap config user.email "github-actions[bot]@users.noreply.github.com"

      # The desktop .dmg is published by publish_desktop (a dependency of this job),
      # so its release asset URL resolves and the release command regenerates the
      # kent-desktop cask alongside the formula.
      - name: Update tap formula and cask
        env:
          KENT_VERSION: ${{ needs.prepare_release.outputs.tag }}
          DESKTOP_DMG_URL: https://github.com/${{ github.repository }}/releases/download/${{ needs.prepare_release.outputs.tag }}/Kent_${{ needs.prepare_release.outputs.version }}_aarch64.dmg
        run: |
          just release brew \
            --tap "$GITHUB_WORKSPACE/homebrew-tap" \
            --formula "$BREW_FORMULA_NAME" \
            --desktop-url "$DESKTOP_DMG_URL"

      - name: Ensure pr-pull label exists
        uses: actions/github-script@v9
        with:
          github-token: ${{ secrets.RESPAWN_BREW_TAP_TOKEN }}
          script: |
            const owner = "respawn-llc";
            const repo = "homebrew-tap";
            const name = "pr-pull";
            try {
              await github.rest.issues.getLabel({ owner, repo, name });
            } catch (error) {
              if (error.status !== 404) throw error;
              await github.rest.issues.createLabel({
                owner,
                repo,
                name,
                color: "0e8a16",
                description: "Ready for brew pr-pull",
              });
            }

      - name: Create tap PR
        uses: peter-evans/create-pull-request@v8.1.1
        with:
          token: ${{ secrets.RESPAWN_BREW_TAP_TOKEN }}
          path: homebrew-tap
          base: master
          branch: chore/${{ env.BREW_FORMULA_NAME }}-${{ needs.prepare_release.outputs.tag }}
          commit-message: "${{ env.BREW_FORMULA_NAME }} ${{ needs.prepare_release.outputs.tag }}"
          title: "${{ env.BREW_FORMULA_NAME }} ${{ needs.prepare_release.outputs.tag }}"
          body: "Automated formula bump for ${{ env.BREW_FORMULA_NAME }} ${{ needs.prepare_release.outputs.tag }}."
          labels: pr-pull
