import { apiCompositionValue } from "@/api/composition";
import { apiValue } from "@/api";
import { facadeValue } from "@/app-facade";

export const fixtureValue = [apiCompositionValue, apiValue, facadeValue];
