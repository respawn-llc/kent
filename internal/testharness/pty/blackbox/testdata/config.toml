model = "gpt-5"
connection = "stub"

[connections.stub]
protocol = "responses"
endpoint = "http://127.0.0.1:1/v1"

[connections.stub.provider_capabilities]
provider_id = "pty-responses-stub"
supports_responses_api = true
supports_prompt_cache_key = true
